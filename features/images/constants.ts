/** Quick-pick target lengths for `videoDurationSeconds` on render. */
export const VIDEO_DURATION_PRESETS = [15, 20, 30, 60] as const;

export const DEFAULT_VIDEO_DURATION_SECONDS = 20;

export const MIN_SCENE_COUNT = 1;
export const MAX_SCENE_COUNT = 300;

export const STUDIO_TEMPLATE_STORAGE_KEY = "studio_template_key";
